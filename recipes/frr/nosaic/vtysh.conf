! vtysh's own configuration.
!
! Present because vtysh refuses to start quietly without it: every invocation
! otherwise prints "Can't open configuration file /etc/frr/vtysh.conf", which
! is noise on a working switch and hides a real error on a broken one.
service integrated-vtysh-config
